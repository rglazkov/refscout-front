# Versions are pinned exactly rather than by range (M0.1.4): pdf.js and
# citation-js parse untrusted input, and a minor version that drifted in
# production is worse than an update that was never applied.
save-exact=true
