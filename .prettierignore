node_modules
.next
out
package-lock.json
playwright-report
test-results

# Generated from the contract - the generator owns the formatting, not us.
*.gen.ts
src/lib/api/wire

# Lifted verbatim out of the API specification by scripts/extract-contract.mjs.
# Reformatting it here would make every re-extraction a diff.
contract

# Regenerated by scripts/sync-fonts.mjs on every build, so hand formatting it
# would be undone by the next one.
src/app/fonts.css

# Generated by `msw init`: the mock's service worker is vendor code, and it is
# committed rather than downloaded at build time (M1.7.3).
public/mockServiceWorker.js
