shadcn/ui primitives: generated by the CLI and edited by hand.

`blocked-button.tsx` is the exception - it is ours. It is a control for an
action that cannot run, and it exists because there are no disabled buttons in
this product (§14): a blocked one stays focusable, says why when it is pressed,
and records the attempt.
