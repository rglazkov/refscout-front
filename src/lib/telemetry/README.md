Client events: numbers, flags and enumerations only - document contents never appear here.

The queue lives in IndexedDB and a record leaves it on a confirmed send, never on
an attempt. Nothing here may import the API layer or the registry of texts, so the
one thing the sender cannot work out for itself - which server answers - is handed
in by the shell when the collectors are attached to the page.
