"Report a problem": the form, the button that opens it from the footer of every
page and from every state of an error, and the switch that stops the automatic
reports.

Before anything is sent the form lists every field with the value it holds, and
what the person unticks is never put into the body. A fragment of their text is
attached only if they selected it themselves and confirmed.
